version: '3.8'
services:
  oracle-db:
    image: gvenzl/oracle-xe:21-slim
    container_name: oracle-prueba-tecnica
    ports:
      - "1521:1521"
    environment:
      ORACLE_PASSWORD: TuPasswordSegura123
      APP_USER: rentcar
      APP_USER_PASSWORD: rentcar123
    volumes:
      - oracle-data:/opt/oracle/oradata
volumes:
  oracle-data: